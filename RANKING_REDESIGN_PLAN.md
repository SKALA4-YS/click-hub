# 메이커 랭킹 전환 계획

## 목표

- 홈에서 프로젝트 `Top 100`과 `HOT` 섹션을 제거한다.
- 프로젝트 썸네일과 상세 화면에서 프로젝트 순위를 노출하지 않는다.
- `/rankings`를 프로젝트가 아닌 프로젝트 공급자(메이커) 랭킹으로 사용한다.
- 프로젝트 탐색과 검색은 `/projects`에서 담당한다.

## URL 구조

| URL | 역할 |
| --- | --- |
| `/` | 최신 프로젝트와 팔로잉한 메이커의 프로젝트 |
| `/projects` | 프로젝트 탐색 및 검색 결과 |
| `/projects/:id` | 프로젝트 상세 |
| `/rankings` | 메이커 랭킹 |
| `/rankings/developers` | 기존 링크 호환을 위해 `/rankings`로 리다이렉트 |
| `/developers/:id` | 메이커 상세 |

## 구현 범위

1. 홈에서 프로젝트 랭킹 조회와 Top 100 섹션을 제거한다.
2. 공통 프로젝트 카드의 순위 prop과 썸네일 순위 배지를 제거한다.
3. 프로젝트 목록을 `/projects`로 이동하고 최신 공개일 순으로 표시한다.
4. 검색 결과, 최신 프로젝트 더보기, 프로젝트 상세 breadcrumb를 `/projects`로 연결한다.
5. 기존 개발자 랭킹 화면을 `/rankings`에 연결하고 사용자 문구를 `메이커 랭킹`으로 통일한다.
6. 프로젝트 상세 및 메이커 상세에서 프로젝트 순위 표현을 제거한다.

## 백엔드 영향

- 메이커 랭킹은 기존 `GET /v1/rankings/developers`를 사용한다.
- 현재 응답의 `rank`, `creatorId`, `displayName`, `score`를 사용하므로 DB나 API 변경은 없다.
- `GET /v1/rankings/projects`는 기존 API 호환성을 위해 유지하지만 프론트에서는 사용하지 않는다.

## 검증 기준

- 홈에 `Top 100`, `HOT`, 프로젝트 순위 배지가 표시되지 않는다.
- `/rankings`에서 메이커 순위와 활동 점수가 표시되고 메이커 상세로 이동할 수 있다.
- `/rankings/developers` 접근 시 `/rankings`로 이동한다.
- 검색 결과가 `/projects?q=검색어`로 이동한다.
- 프로젝트 목록과 상세에 프로젝트 순위 문구가 표시되지 않는다.
- 프론트 테스트, Lint, Prettier 검사와 프로덕션 빌드를 모두 통과한다.
